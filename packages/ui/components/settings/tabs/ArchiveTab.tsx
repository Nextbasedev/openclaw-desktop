"use client"

export function ArchiveTab() {
  return (
    <div className="rounded-xl border border-border/50 bg-card/60 p-6 text-sm text-muted-foreground">
      Archive UI was removed with the old chat/sidebar data layer. It can be rebuilt cleanly with the new middleware-backed UI.
    </div>
  )
}
