console.log(`Remote mode does not start local middleware.

1. Install middleware on the remote machine:
   bash scripts/install-middleware.sh

2. Open Desktop/Web and paste the Middleware URL + token.

For local development of the UI only, run:
   pnpm dev:web
`)
